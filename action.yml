name: tool1 main action

description: 'Tool 1 main action'

inputs:
  name1:
    description: 'name1'
    type: string
    required: true

  name2:
    description: 'name2'
    type: string
    required: true

  secret1:
    description: 'secret1'
    type: string
    required: true

  secret2:
    description: 'secret2'
    type: string
    required: true

runs:
  using: 'composite'
  steps:
    - name: 'test'
      shell: bash
      run: |
        echo "!! main Action !!"
